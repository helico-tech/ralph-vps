# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Path to the target project repository (absolute path on host)
PROJECT_PATH=/home/ralph/projects/my-project

# Optional: Override config values via environment
# RALPH_MODEL=claude-opus-4-5
# RALPH_MAIN_BRANCH=main
# RALPH_PERMISSION_MODE=skip_all
