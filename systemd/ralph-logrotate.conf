# Logrotate config for Ralph journal logs exported to file
# Only needed if exporting journal to file via journalctl > /var/log/ralph.log

/var/log/ralph/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
