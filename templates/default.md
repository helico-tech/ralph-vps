# Task — {{id}}

**Type:** {{type}}

{{description}}

## Workflow

1. Read and understand the task description above.
2. Explore the relevant files to understand the current state of the code.
3. Make the changes described. Stay within scope — do not refactor unrelated code or add features not requested.
4. Write tests for any new functionality. Follow existing test patterns.
5. Run the full test suite. All tests must pass.
6. Stage only the files you changed (never `git add -A` or `git add .`). Commit with message: `ralph({{id}}): <brief description>`.
