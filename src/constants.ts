export const RALPH_DIR = ".ralph";
export const RALPH_LOCAL_DIR = ".ralph-local";
export const TASKS_DIR = `${RALPH_DIR}/tasks`;
export const TYPES_DIR = `${RALPH_DIR}/types`;
export const LOGS_DIR = `${RALPH_LOCAL_DIR}/logs`;
export const STATE_FILE = `${RALPH_LOCAL_DIR}/state.json`;
export const SYNC_MANIFEST_FILE = `${RALPH_LOCAL_DIR}/sync-manifest.json`;

export const PROJECT_CONFIG_FILE = `${RALPH_DIR}/project.json`;
export const NODE_CONFIG_FILE = `${RALPH_DIR}/node.json`;
export const CLIENT_CONFIG_FILE = `${RALPH_DIR}/client.json`;

export const TASK_FILE_EXTENSION = ".md";
export const LOG_FILE_EXTENSION = ".json";
